# Hidden paths are private/local by default.  The ignore rule itself is the only exception.
.*
!.gitignore

# Local secrets and environments
.env
.env.*
# ★`.env.*` 가 `.env.example` 까지 막고 있었다. 그래서 팀 저장소에
#   설정 예시가 하나도 없었다 — 받는 사람이 무엇을 채워야 할지 모른다.
#   예시 파일은 비밀이 아니므로 예외로 연다(값이 아니라 **키와 설명**이다).
!.env.example
.venv/
venv/

# Python/test caches
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/

# Editors and operating systems
.idea/
.vscode/
.DS_Store
Thumbs.db

# Runtime databases, logs, models and generated artifacts
*.sqlite3
*.sqlite3-*
*.db
*.log
*.onnx
*.pt
*.pth
*.bin
*.gguf
artifacts/
backups/
legacy/

# Internal documents and copyrighted/private/large data
docs/
# ★backend 브랜치는 백엔드 핸드오프 문서를 함께 싣는다.
#   docs/ 를 통째로 막아 두면 계약서가 코드와 따로 놀게 된다 —
#   실제로 이 브랜치 첫 커밋에서 문서 6개가 조용히 빠졌다.
!docs/
!docs/handoff/
!docs/handoff/*.md
data/*
!data/raw/
data/raw/*
!data/raw/manifests/
!data/raw/manifests/*.jsonl

# Temporary files and archives
*.bak
*.backup
*.tmp
*.zip
*~
