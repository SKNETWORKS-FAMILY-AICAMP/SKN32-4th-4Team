# 인수인계 문서 (handoff)

팀 **비서단** — 올바른 보험비서. 각자 작업에 필요한 확정 사항을 모아 둔다.

> 여기 있는 것은 **합의된 계약**이다. 바꾸려면 팀에 알리고 버전을 올린다.
> 리포트(`docs/reports/`)는 "무슨 일이 있었나"의 기록이고, 여기는 "무엇을 지킬 것인가"다.
> `docs/` 전체(plans·history·reports·TODO 등)가 뭐가 뭔지 헷갈리면 [../README.md](../README.md) 부터 본다.

---

## 문서 목록

| 문서 | 대상 | 내용 |
|---|---|---|
| [01_데이터_현황.md](01_데이터_현황.md) | 전원 | 지금 무엇이 있고 무엇을 믿을 수 있나 |
| [02_ERD_및_스키마.md](02_ERD_및_스키마.md) | 백엔드 | 엔티티·관계·DDL — ★**2026-08-02 전면 개정** |
| [03_에이전트_데이터_축적_설계.md](03_에이전트_데이터_축적_설계.md) | 전원 | 외부 에이전트 데이터를 어떻게 받고 쌓고 쓰나 |
| [04_계약_AI1_검색.md](04_계약_AI1_검색.md) | 서유현 | 검색 담당 인터페이스 |
| [05_계약_AI2_판정.md](05_계약_AI2_판정.md) | 송채영 | 판정·설명 담당 인터페이스 |
| [06_계약_Agent.md](06_계약_Agent.md) | 정재희 | LangGraph·MCP 담당 인터페이스 |
| [07_계약_백엔드.md](07_계약_백엔드.md) | 김지혜 | DB·API·감사 담당 인터페이스 |
| [08_계약_프론트.md](08_계약_프론트.md) | 최연우 | 화면 상태표 |
| [09_A2A_판단.md](09_A2A_판단.md) | 전원 | 에이전트 간 위임을 할 것인가 |
| [10_계약_모델_평가.md](10_계약_모델_평가.md) | 서유현·송채영 | **모델 평가 지표·평가셋·색인 입력 계약** |
| [11_AI_구조_지도.md](11_AI_구조_지도.md) | 전원 | ★**RAG는 몇 개인가 · 무엇이 무엇을 근거로 쓰나 · 역할 구분** — 반복 질문 한 장 정리 |
| [12_모델팀_개선사항.md](12_모델팀_개선사항.md) | 서유현·송채영 | ★**`feature-ai1` 점검 결과 — 재색인 전 고칠 것** (P0 1건 · P1 7건 · P2 2건) |
| ★[13_임베딩모델_선정_브리핑.md](13_임베딩모델_선정_브리핑.md) | 서유현·송채영 | ★★**배포 확정(2026-08-27)** — `dragonkue/snowflake-arctic-embed-l-v2.0-ko`, §3-3-★★ 참조. 측정 21회 + 짝비교 신뢰구간 + s6/holdout 재검증(3회 연속 확인된 열세 없음)이 근거이나, **δ·구어체 질의는 여전히 안 쟀다** — 팀이 그 절차를 생략하고 정책적으로 확정한 것이지 비열등성이 수치로 증명된 게 아니다. §5 를 함께 읽을 것 |
| [임베딩 s6코퍼스·holdout 재검증](../reports/2026-08-27_0139_임베딩_s6코퍼스_홀드아웃_재검증.md) | 서유현·송채영 | **완료(2026-08-27), 배포 확정의 근거 리포트.** §15 s6 재생성 직후 평가셋을 s6로 재구축(+holdout, 겹침 0건 확인) → GPU(RTX 4000 Ada)에서 8후보 재측정. 상위 3파전(`arctic-ko`·`BGE-m3-ko`·`KURE-v1`)은 s6·holdout 양쪽에서 통계적으로 계속 구분 안 됨(순위 역전 없음). `Qwen3-Embedding-4B`는 tail·exclusion 확인된 열세 + 배치16 CUDA OOM + 2.5배 느림으로 탈락 |
| ★[Qwen3-4B fp16·4bit 동일GPU 짝비교](../reports/2026-08-26_0940_Qwen3_4B_fp16_4bit_동일GPU_짝비교.md) | 서유현·송채영 | **13번 §5-6이 스스로 지적한 GPU 교란변수(fp16=RunPod·4bit=x600)를 없앤 재측정.** x600에서 fp16 전체 재측정 + 새 RunPod(RTX 4000 Ada)에서 독립 3중 교차검증까지 — 세 GPU 전부 같은 결론(4bit이 성능을 떨어뜨린다는 증거 없음) |
| ★[Codex 대체 5파트 최종점검](../reports/2026-08-26_코덱스대체_5파트_최종점검.md) | 전원 | **API 키 없이 5파트(LLM관측·과금보호·부하드라이버·S7골든평가·독립검증) 전부 재검증 완료(2026-08-26).** 219 passed·providerless 부하 80/80·S7 12/12 case·36/36 run. 원본 워커 보고서 5개(`worker-2/3/5-*.md`, `2026-08-25_1756_worker1_...md`, `2026-08-25_2350_worker4_...md`)와 통합보고([0345](../reports/2026-08-26_0345_원격5파트_스트레스통합_완료보고.md))도 같이 커밋됨. ★파트2(ZIP)는 현재 통합본보다 이전 계약이라 미적용, 파트4(ZIP)는 선별 통합 대기 — [00_진행현황_통합검수.md](../../../_3rd_project_4/stress_test_1/00_진행현황_통합검수.md) 참조(그 문서 자체는 이 저장소 밖) |
| ★[18_브랜치_반영_내역.md](18_브랜치_반영_내역.md) | 전원 | **팀 브랜치에서 무엇을 가져왔고 무엇을 안 가져왔나** — 거절 사유까지. §6 에 팀에 알릴 것 4건 |
| ★[19_담당자별_코드범위_및_구현현황.html](19_담당자별_코드범위_및_구현현황.html) | 전원 | **비전공자용 60초 요약·쉬운 용어사전과 함께, 5명 담당자의 폴더·파일 From→To 책임 경계, 공동 승인, 구현 현황, 무담당 트랙을 한 화면에 정리한 브리핑** |
| ★[20_문서확정_미확정107건_검토목록.html](20_문서확정_미확정107건_검토목록.html) | 전원 | **사람이 직접 골라야 하는 문서 107건** — 원인(빠진 토큰·경쟁 상품)별로 묶고 각 건마다 공시 PDF 링크 + 로컬 경로(`data/raw/insurance_terms/…`)를 붙였다(브라우저로 열기). 실측 2026-08-14 |
| ★[PostgreSQL 런타임 운영성 재점검](../reports/2026-08-24_postgresql_런타임_운영성_재점검.md) | 백엔드·전원 | **"pgvector가 PG니까 전체 앱도 PG 운영형이다"는 틀렸다** — `AUTH_PERSISTENCE`·`OPS_PERSISTENCE`=sqlite, `PRECHECK_PERSISTENCE`=off, `CLAUSE_STORE`=file 이 **개발 기본값**이다. PG 경로는 구현됐지만(어댑터·스키마 001~016) 운영 기본값 통일·connection pool·컨테이너 배포는 아직이다. 정확한 한 줄: "PG adapter는 구현, 기본값은 아직 SQLite/file" |
| [s6 전량재생성 후 manifest_s6 불변보증 깨짐 — 수정 완료](../reports/debugs/2026-08-26_1715_s6_전량재생성으로_manifest_s6_불변보증_깨짐.md) | 전원 | **해결됨(2026-08-26 17:29).** 옛 매니페스트를 백업(`data/backup/manifest_s6_backup_2026-08-04_before_force_20260826_1729.json`)한 뒤 `--force`로 재기준선화, `--verify` 재통과 확인(문서 1,355·어긋남 0). 라이브 조항 벡터 인덱스는 아직 옛 s6 스냅샷 그대로(별도 재색인 필요, 미착수) |
| [S3잔여26건 Codex분석 새오탐2종 — 수정 완료](../reports/debugs/2026-08-26_1730_S3잔여26건_Codex분석_새오탐2종_발견.md) | 전원 | **해결됨(2026-08-26 20:40), 디스크 반영도 완료.** `struct_audit.py`에 S3 전용 예외 2개 추가(조항 경계 자체는 안 바뀜) — S3_embedded_header 26→**9**, gated 1,420→1,403(법령나열 인접 괄호 8건·약관요약서 쉼표종결 참조 9건 오탐 제거). 코드 수정분은 아래 s6 전량재생성(23:20)에 함께 반영됨. 완료보고 원문: [2040](../reports/2026-08-26_2040_S3잔여26건_수정_완료보고.md) |
| [§15 자기참조 타조인용 3차시도 — 완료](../reports/2026-08-26_2255_S15_자기참조_타조인용_3차시도_완료보고.md) | 전원 | **해결됨(2026-08-26).** `_REF_TAIL`/S3 판정식은 안 건드리고 heads 루프 안에서만 좁게 뒤집는 3차 설계로 구현, 코덱스 5.6sol이 diff를 직접 검토·재현해 찾은 결함 4건(역순 법령표지 미인식·동일이름 부 재진입 합산·제목 공백 미정규화·가지번호 비교 누락)까지 전부 고침. 전량실측 `S1_aba_reentry` 716→714·`S3_embedded_header` 9→**1**·`gated_clause_count` 1,403→1,391(전부 개선, 악화 없음), 신규 회복 조항 +3,327. 사람 표본 10건 원문대조 오탐 0건. 코드는 git 커밋 `e0c509ae` |
| ★[s6 전량재생성(§15 반영) + manifest 재기준선화 — 완료](../reports/2026-08-26_2325_s6_전량재생성_S15반영_완료보고.md) | 전원 | **해결됨(2026-08-26 23:20).** 백업(tar 142MB, 개수 확인 후 삭제) → `run_all --stage clauses` 재생성(1,355/1,355, 실패 0) → 디스크 집계가 §15 코드측정치와 정확히 일치(`aba` 714·`S3` 1·`annex` 0·`gated` 1,391) 확인 → manifest_s6 백업 → `--force` 재기준선화 → `--verify` "문서 1,355 대조 · 어긋남 0". 백업 목록: `data/backup/s6_pymupdf-1.28.0_backup_20260826_2316_before_S15.tar.gz`·`data/backup/manifest_s6_backup_before_S15_20260826_2320.{json,sha256}`. ★라이브 조항 벡터 인덱스는 여전히 이전 스냅샷 기준(별도 재색인 과제, 미착수) |
| [s5L 단계2 완료보고](../reports/2026-08-27_s5L_단계2_완료보고.md) | 모델팀 | **완료(2026-08-27).** 부 제목 크기/굵기 확인 신호 — 코덱스 5.6sol "사람 표본검수자" 시뮬레이션이 1차 구현(조 머리 대리지표)을 보류시킴 → 부 제목 실제 모집단으로 12개사 전량 재측정, 활성 5→9개사 재조정, 버그 4건 수정. 판정 불변(설계대로), 정보용 stats만 추가. 기존 결함 2건(NH 러닝풋터 오채택·러닝헤더 veto가 `_SECTION_TITLE` 공백흡수로 우회) 발견·기록만(범위 밖) |
| [archive/…_v1_재측정전.md](archive/13_임베딩모델_선정_브리핑_2026-08-03_v1_재측정전.md) | — | ☠**폐기된 이전 판.** 인용 금지 — 무엇이 왜 틀렸는지 남기려고 보관 |
| [14_금감원_분쟁사례_평가셋_작업계획.md](14_금감원_분쟁사례_평가셋_작업계획.md) | 서유현·송채영 | 분쟁사례 기반 평가셋 |
| ★[15_표_정답셋_라벨링_의뢰.md](15_표_정답셋_라벨링_의뢰.md) | **서유현(AI 1)** | **표 과탐 판별기 재료 + 층화 정답셋** — 사람이 찍어야만 되는 것 |
| ★[16_DB_스키마_적재_의뢰.md](16_DB_스키마_적재_의뢰.md) | **김지혜(백엔드)** | **`app.*` DDL 전체 + 적용 + 데모 한 줄기** — 스키마 계약 확정 |
| ★[17_LangGraph_인용검증_MCP_결함수정_의뢰.md](17_LangGraph_인용검증_MCP_결함수정_의뢰.md) | **정재희(Agent)** | **재현 결함 3건 + 문서/0근거 게이트 + 그래프 정본 통합** |
| [erd_briefing.html](erd_briefing.html) | 전원 | ERD·스키마 브리핑 — **왜 이렇게 설계했나**(브라우저로 열기) |
| [erd_tables.html](erd_tables.html) | 백엔드·전원 | ★**테이블·컬럼 사전** — 27테이블+뷰1의 전 컬럼·타입·제약·enum(브라우저로 열기) |
| [system_diagrams.html](system_diagrams.html) | 전원 | ★**시스템 시각화 6장** — 아키텍처·오프라인/온라인·기권 게이트·실패 전파·검증 확장·12GB 예산(브라우저로 열기) |
| [preprocess_viz.html](preprocess_viz.html) | 모델팀·전원 | ★**전처리 s6 산출물 시각화** — 그림 7장 + DB 적재 정합성 + Tad 행 단위 캡처 5장(브라우저로 열기). ★캡처만 10:05 판이라 낡음 배너가 붙어 있다 |
| ★[구조결함 S1·S2 원인규명](../reports/2026-08-03_2330_구조결함_S1_S2_원인규명.md) | 모델팀·백엔드 | **게이트가 멀쩡한 조항을 끄고 있다** — S1 87.2% 는 부 경계 누락, S2 41.9% 는 결함 아님. 사건 파일 위치·재실행법 포함 |
| ★[24_팀원_배분가능_작업목록.md](24_팀원_배분가능_작업목록.md) | 전원 | **지금 넘길 수 있는 일** — 사람 판단 **600건**(표라벨 125 · B8 26패턴 · F4 9 · 조항밖 440) + 검증·촬영 7건. §0 은 이미 배분된 셋, §9 는 확인해서 닫은 것 |
| ★[25_사람검수600건_5인_시간균형_HTML](review_600_5parts/index.html) | 전원 | **건수가 아니라 예상 검수시간으로 나눈 5인용 HTML** — 파트 1~4 약 2시간 29~30분, 파트 5는 B8·F4 중요 작업 전용 약 2시간 23분. 자동 임시저장·미완료 필터·JSONL 반환 지원. 선택 시 `✓ 선택됨`과 현재 선택을 표시하며 종전 저장값도 자동 복구. `01_모범선택사례.html`에 파트별 쉬운 예시 16개 수록 |
| [QA 파일럿 Part 5 Codex 선검수 결과](../reports/2026-08-26_1929_QA파일럿_part5_Codex선검수_및_사람재검큐.md) | 모델팀·Part 5 담당 | ☠**낡음 — 이후 [1953](../reports/2026-08-26_1953_QA파일럿_part5_날짜오류10건_재생성.md)→[2039](../reports/2026-08-26_2039_QA파일럿_part5_항목별_재감사.md)에서 A13·E47까지 재조정되고, 아래 [QA 파일럿 검수결과 통합과 재배포](../reports/2026-08-27_QA파일럿_검수결과_통합과_재배포.md)가 최신 상태다.** (원문 그대로 보존) 60건 전수 1차 검수 완료 — A 33·문장수정 17·날짜충돌 반려 10. Codex 1차 검수이며 사람 최종 승인으로 표시하면 안 됨 |
| ★[s6 재생성 후 색인 영향 실측](../reports/2026-08-26_2050_s6_재생성후_색인영향_실측.md) | 백엔드·모델팀 | **재색인을 언제 할지 정하는 데 필요한 수치.** ☠**§0 정정 필수** — 「순번 자리 89.3% 불일치」 논거는 **무효**다(산출물은 전 조항에, 색인은 게이트 통과분만 0부터 다시 번호를 매기는 **다른 체계**라 낡음과 무관하게 어긋난다). 색인의 인용가능 발생 중 **32.6%가 현행 산출물에 없는 내용**. ★단 `core.clause_reference`·`app.assessment_clause_citation` 이 **0행이라 소급 피해자는 없다** — 순번을 바꾸기에 가장 싼 시점이다. 새 해시 23,782개 임베딩 필요(65.8% 재사용). ★s5L 연동이 s6 를 또 바꾸므로 실행 시점은 사람이 정할 일이었으나, **2026-08-27 확인 — s5L 단계1은 이미 배선·소비돼 있고 s6 출력에 실질 영향 0건**이라 이 우려는 해소됐다(위 [s5L 전량생성 실행결과](#) 정정 참조). s6 재생성·재색인 자체는 이미 완료됨(§15 반영, 아래 보고서들 참조) |
| ★[26_전달계층_Django분리_비교분석.md](26_전달계층_Django분리_비교분석.md) | 백엔드·전원 | **Django 를 말단 전달 계층으로만 두는 형상의 실측 비교.** 두 기계에서 쟀다 — 홉 하나의 고정비는 **1.4~3.8ms** 이고, 그것이 무시할 만한지는 **판정 경로가 얼마나 빠르냐에 달렸다**(RunPod 253ms 기준 ±5.3% vs x600 20ms 기준 **+9~15%**). ★**keep-alive 는 플랫폼에 따라 결론이 뒤집힌다** — Linux 루프백에서는 없어도 그만이지만 Windows 에서는 끄면 판정이 **2.2배** 느려진다. 단위 테스트 38건이 전부 통과하는 상태에서 **모든 요청이 실패**하고 있었던 함정 6종도 정리했다 |
| ★[27_LLM생성모델_선정_브리핑.md](27_LLM생성모델_선정_브리핑.md) | 전원 | **생성 LLM 선정 전 과정을 하나로 합침**(2026-08-02 SOTA 조사 → EC2 디스크 제약으로 OpenAI 전환 → `gpt-4.1-nano` 채택 → 로컬 12종 재검토, 전부 `gemma4:e4b`(40%)를 못 넘음). §8에 남은 작업 표 |
| ★[s6 재색인 GPU 임베딩 작업계획·기록](../plans/2026-08-26_2130_s6_재색인_GPU임베딩_작업계획.md) | 백엔드·모델팀 | **산출물이 어디 있고 무엇으로 만들었나.** GPU 호스트 `/workspace/reindex_20260826/`(그 안에 README 동봉) · 조항 19,771 → 조각 55,203 · md5 대조 · 기대 벡터 226,111,488바이트. ★대상 수가 23,782 과 19,771 로 갈린 4,011 의 정체를 갈라 적었다(parse_status 660 · 인용게이트 3,351 · 부록 17 — 조용한 스킵 아님). ★★기존 색인 벡터와 **코사인 0.999846 이상**이라 다른 GPU 산 벡터와 섞어 쓸 수 있음을 확인. 적재·순번·HNSW 는 §7 |
| [관리자 LLM 프로바이더 선택 UI 완료보고](../reports/2026-08-26_2258_관리자_LLM프로바이더_선택_UI_완료보고.md) | 백엔드·프론트 | `GET/PUT /api/admin/llm-provider` 신설 — 판정 모드 스위치와 같은 패턴(파일 기반 오버라이드, admin·customer 프로세스 분리 대응). 화면(`admin.html`/`admin.js`)까지 실제 조작 검증 완료. 신규 파일 1(`app/core/domain/llm_provider_override.py`) + 기존 3개 수정(`llm_clients.py`·`admin.py`·`admin.html`·`admin.js`) |
| ★★[s6 재색인 완료보고 — 신선도 0](../reports/2026-08-27_0200_s6_재색인_완료보고.md) | 백엔드·모델팀 | **색인이 현행 s6(23:18판)과 같은 판이 됐다.** 낡은 해시를 든 문서 1,172/1,302 → **0/1,336**. `-m pg` 49/49 통과(시작 때 실패하던 2건 포함). 조각 223,372·임베딩없음 0·고아 0. ★도중에 안전장치 둘이 **서로 반대 방향으로** 틀린 것을 찾아 고쳤다 — `reconcile_occurrences` 는 너무 엄격해 64,171행 전부를 막았고, `check_index_freshness` 는 너무 물러 OCR fact 850행을 낡음으로 셌다. §3 |
| ★[28_DB4_core원장_적재_승인요청.html](28_DB4_core원장_적재_승인요청.html) | **팀장·백엔드·전원** | **`core` 원장이 왜 0행인가 — 브라우저로 열기.** ★「사람 승인만 하면 되는 상태가 아니다」 — 승인 말고도 **배선 3곳**이 끊겨 있다(도구가 dbins 236건만 봄 · 산출물 `identification` 전부 `unidentified` · `release_state.approval` 없음/candidate). 게다가 서명할 `ops.admin_user` 가 0행. 확정 1,355건 **전부** `사람 최종승인 대기`이고 그중 **name_only 945건(69.7%)**·`extraction_blocked` 41건이 가장 약한 근거다. 승인 시 들어갈 양·보험사별 분포·누가 무엇을 할지까지 |
| ★[Qwen3-4B fp16·4bit RunPod RTX2000 통제측정](../reports/2026-08-26_0020_Qwen3_4B_fp16_4bit_동일GPU_통제측정_리포트.md) | 모델팀 | [0940 짝비교](../reports/2026-08-26_0940_Qwen3_4B_fp16_4bit_동일GPU_짝비교.md)가 인용한 "세 GPU 전부 같은 결론" 중 RunPod RTX2000 Ada 축 원본 실측 — 제목·뒷부분·면책 **세 축 모두 95% 구간이 0 포함**(차이 미확인). ★부수 발견: `batch`가 결과 JSON 24건 전부에 기록 안 됨, `--compare` 도구는 fp16/4bit를 같은 모델명으로 덮어써 항상 "비교 0개" |
| ★[개념 스키마가 비어 있다 — 근거 있는 판정은 PG에 저장 안 됨](../reports/2026-08-26_0100_개념스키마가_비어_있어_근거있는_판정이_저장되지_않는다.md) | 백엔드·전원 | `insurance_real.core`(정본원장) 0행은 승인 전 fail-closed 설계지만, 그 결과 **근거 인용이 붙은 판정은 `CLAUSE_STORE=pg`에서 저장 자체가 실패**한다(`InfraError`). `mall_vec`은 아직 `core`의 파생이 아니라 s6 산출물에서 직접 생성 — `core`→`mall_vec` ETL이 코드로 존재하지 않음 |
| [CLAUSE_STORE=pg 전환 판정 동등성 실측](../reports/2026-08-26_0130_CLAUSE_STORE_pg_전환_판정_동등성_실측.md) | 백엔드 | 확정 1,296문서 중 무작위 25문서×4질병기호=100건에서 `file`/`pg` **결론·인용개수 완전 일치**(불일치 0, 실판정 42건 포함). 성능·전량 커버리지는 미측정 — 전환 확정 아님 |
| [공개챗봇 과금보호 3채널 E2E 완료](../reports/2026-08-26_0132_공개챗봇_과금보호_3채널_E2E_완료보고.md) | 백엔드·Agent | 공개 REST·등록에이전트 REST·MCP 세 채널이 동일 계약·동일 fake provider 결과 공유 확인(149개 시험군+동시 20건×5회 통과). 예산 소진 시 429/`Retry-After`, MCP `http_status=429` 일관. ★현재 uvicorn 단일 worker 기준 보호라 replica 확장 시 공유 제한 별도 필요 |
| ★[판례 114골든셋 검토현황 점검 + 미완료14건 AI·Codex교차검증 + 전량원장반영](../reports/2026-08-26_0940_판례114골든셋_검토현황_점검과_미완료14건_AI교차검증.md) | 전원(법률검토) | 100건 완료로 알려졌던 검토가 **원장엔 전부 `unreviewed`로 미반영**이었음을 발견. 미완료 14건 AI+Codex 교차검증 후 사용자 확정으로 반영, **114건 전량 `human` 반영 완료**(confirmed 57·corrected 30·rejected 27). ★`case_100`·`case_94`는 기존 Codex 선검토와 결론이 갈려 팀 확인 필요 |
| ★[사람검수 600건 Part1~5 전량 점검 + 최종 병합 완료](../reports/2026-08-26_1103_사람검수600_part3_part4정정_최종병합보고.md) | 전원 | 600건 전량(중복0·누락0) `human_review_600_merged.jsonl`로 병합. Part1 20건·Part3 9건·Part4 **70건**(총 99건) 신규 정정, Part2·5는 기존 정정(141/35건) 유지. ★★Part4는 **사람 원안이 아니라 Codex가 원문 전수대조로 정정한 결과**라 "사람이 최종 승인한 600건"이라 표현하면 안 됨 — 사람 승인이 필요하면 Part4 변경 70건만 하면 됨. (앞선 [0424](../reports/2026-08-26_0424_사람검수_파트2_파트5_정정완료보고.md)·[1038](../reports/2026-08-26_1038_사람검수600_통합기록과_part1점검_보고.md)의 정정은 여기 흡수됨) |
| ★[front 브랜치 UI 비교분석](../reports/2026-08-26_1146_front브랜치_UI_비교분석과_반영범위.md) | 프론트·전원 | **디자인은 가져오고 JS 로직은 가져오면 안 된다** — team4/front를 그대로 덮으면 Idempotency-Key·incident_on·CSRF 누락으로 운영모드 판정이 전부 막힌다(회귀 5건, R1~R5) + 모바일 사이드패널 접근불가 등 디자인 자체 결함 5건(D1~D5). §6 권장 순서대로 실제 이식은 같은 날 커밋 `1d826af4`로 진행됨(확정문서 57건 누락 결함도 그 과정에서 별도 발견·수정) |
| [s5L 전량생성 실행결과](../reports/2026-08-26_1330_s5L_전량생성_실행결과.md) | 모델팀 | **줄 단위 레이아웃 포함 페이지 추출 1,362건 100% 완료**(실패 0, x600 원격 37.65건/분 vs 로컬 8.14건/분). ★★**이 표(2026-08-27 재확인)를 정정한다 — "아직 소비처 없음"은 낡았다.** `to_page_json.SCHEMA_VERSION` 이 이미 `"5L"`로 올라가 있어서 `run_all.py` 가 **이미 s5L을 페이지 소스로 읽는다**(§15 s6 재생성도 s5L에서 나옴, 타임스탬프로 확인). [s5L 연동계획](../plans/2026-08-26_1600_s5L_레이아웃_to_clauses_연동_계획.md) 단계1(러닝헤더 위치+반복)은 배선·소비 둘 다 됨 — 전량 표본 200건 중 139건(69.5%)에서 신호가 실제로 발생하지만, `_SECTION_LINE` 어휘와 안 겹쳐 최종 판정에는 영향 0건(이미 알려진 결과, 결함 아님). ★★**단계2도 완료(2026-08-27)** — [완료보고](../reports/2026-08-27_s5L_단계2_완료보고.md). 코덱스 5.6sol을 "사람 표본검수자"로 세워 실 표본 검수(계획서의 "사람 필요" 조건 충족) → 1차 구현의 조 머리 대리지표가 다수 보험사에서 정반대임을 발견 → 부 제목 모집단으로 전량 재측정, 활성 보험사 5→9개 재조정 + 버그 4건 수정. 판정은 설계대로 불변(S1/S3/S4/gated 전부 §15 기준선과 동일) — 정보용 집계만 stats에 추가. 단계3(§15류에 레이아웃 도움되는지)은 선택·낮은 우선순위, 미착수 |
| [CI PostgreSQL 관문 구축](../reports/2026-08-26_CI_PG관문_구축.md) | 백엔드·전원 | **`postgres-schema-gate` CI 잡 설계 완료 + 로컬 5개 계약검증 전부 통과.** 코덱스가 스스로 예고한 실패(`test_erd_matches_db.py`가 `public` 검사, 마이그레이션은 `vec`)를 실측으로 재현·수정해 `public`·`vec` 양쪽 통과 확인. ★워크플로 자체를 실제 GitHub Actions에서 끝까지 돌린 적은 아직 없음(하위 디렉터리라 자동실행 안 됨도 확인) |
| ★[mall_vec→insurance_real 통합 사전감사](../reports/2026-08-26_mall_vec_통합_사전감사.md) | 백엔드 | **cutover 아직 금지.** 코덱스 감사, P0 7건 중 2건만 즉시 수정(COPY 열순서·소유자), 나머지(맨이름 SQL 14파일/111곳·ledger 모순·`.env` DSN 누락·CI 관문 0개) 미해결. 지금 상태는 `mall_vec`·`insurance_real.vec` 둘 다 살아있고 앱은 여전히 `mall_vec`만 읽음 |
| [s5-mixed 세대 은퇴 판단자료](../reports/2026-08-26_s5mixed_세대은퇴_판단자료.md) | 백엔드·모델팀 | ☠**낡음 — 은퇴 완료됨(2026-08-27, `insurance_real` DB 직접 조회로 확인).** 이 리포트가 "지금 지우면 안 된다"고 막았던 조건(s6 자체 위반 5,791행) 자체가 이번 s6 재색인으로 해소됐다. 실측: `vec.policy_clause_occurrence` **186,561행 전부 `index_generation='s6'`**, `s5-mixed` 값 자체가 테이블에 없고, `occurrence→content` FK 위반 **0건**. `reconcile_occurrences --apply`(64,171행 삭제, [재색인 완료보고](../reports/2026-08-27_0200_s6_재색인_완료보고.md) §2)가 같이 정리한 것으로 보인다 |
| [PostgreSQL 백업 S3→x600 전환](../reports/2026-08-26_x600_백업_전환과_디버깅.md) | 백엔드 | S3 비용을 피해 x600(팀 GPU 박스)이 EC2에 SSH로 pg_dump를 당겨오는 방식으로 전환, 매일 03:00 스케줄러(SYSTEM 계정) 등록·2회 재현검증 완료. **복원 리허설은 아직 미실행** |
| ★[QA 파일럿 300건 — 후보생성과 검수화면](../reports/2026-08-26_승인QA_파일럿300_후보생성과_검수화면.md) | 전원(Part 1~5 검수 담당자) | `/v1/prechecks` 1,248회 실측으로 `verdict×reason_code` 7개 층을 만들어 A180·B60·C60 구성, **`likely_covered` 0건**(실손은 단정 안 함). "기권≠보류" 구분·미완성 수정 경고 등 결함 4건 수정. 검수 결과는 아래 [QA 파일럿 검수결과 통합과 재배포](../reports/2026-08-27_QA파일럿_검수결과_통합과_재배포.md) 참조 |
| ★[전역 파괴 함수 감사](../reports/2026-08-26_전역파괴함수_감사.md) | 백엔드·전원(테스트 작성자) | 코덱스 감사, `drop_incomplete`(P0, 범위 인자 없이 즉시 DELETE+commit)가 최대 위험. 366개 파일 중 파괴 키워드 35개 파일(9.6%) 전수 분류, `ensure_schema` 실데이터 시험 미격리 8/10건 발견. 계기는 [시험이 운영DB에서 drop_incomplete를 돌려 본문 1,506행을 지운 사고](../reports/debugs/2026-08-26_내_시험이_운영DB에_drop_incomplete를_돌렸다.md) |
| ★[QA 파일럿 검수결과 통합과 재배포](../reports/2026-08-27_QA파일럿_검수결과_통합과_재배포.md) | 전원(Part 1~5 검수 담당자·모델팀) | **5파트 검수 240건을 결정 대신 등급만 매겨 재배포.** Part1·2·4는 A축 재정의(검색정답셋→엔진판정) 전 옛 후보 기준이라 133건 폐기, [Part1·2·4·5 원자료재감사](../reports/2026-08-27_QA파일럿_수신본_Part1_2_4_5_원자료재감사.md)도 축단위 일괄결정·반려27건 순환논증 결함이 발견돼 결정으로 안 씀. `disputed`25·`review_only`51·`untouched`55 = **사람 재검 131건**. 검수자 정확도 실측: 송채영 41/41 무결함, Codex 1차는 승인 37건 중 14건 오류. Part3 아직 미수신. (이 계열의 중간 단계: [1953](../reports/2026-08-26_1953_QA파일럿_part5_날짜오류10건_재생성.md)→[2039](../reports/2026-08-26_2039_QA파일럿_part5_항목별_재감사.md)로 Part5만 A13·E47로 정정된 뒤 여기서 전체 재배포됨) |
| ★★[DB 잔여작업 정리 — 판정 원장이 막힌 진짜 이유](../reports/2026-08-27_0600_DB잔여작업_정리와_KCD사전_부재.md) | 백엔드·전원 | **「core 비어서 저장 실패」가 아니었다.** core 를 채우고 끝까지 따라가니 `resolve_kcd_reference` 가 `core.kcd_code` 0행 때문에 죽는다 — 그리고 **KCD 코드→질병명 사전을 우리는 갖고 있지 않다**(약 2만 항목). `name_ko` 는 NOT NULL 이라 지어내면 그 값이 판정 원장에 사실로 남는다. ★**넣지 않았다.** 필요한 것은 DB 작업이 아니라 **통계청 KCD 마스터 자료 확보**다. 함께: 백업 3.0GB 회수(세는 도구가 `*_bak_*` 를 못 보고 있었다) · `core.clause_code_rule` 61,823행 적재(mention 29,424는 성격 불명이라 판정에 쓰면 안 됨) |
| ★★[29_운영_기본값_결정.md](29_운영_기본값_결정.md) | 백엔드·전원 | **무엇을 어디에 두나 — 결정 기록(2026-08-27).** 코드 기본값은 **안 바꾼다**(PG 없는 사람이 앱을 못 띄운다). 운영 설정은 `.env.example` 이 정본이고 `readyz` 가 판정한다. ☠**`PRECHECK_PERSISTENCE=postgres` 는 켜면 모든 판정이 503** — `core.kcd_code` 0행이고 KCD 마스터를 우리가 안 갖고 있다. ★`readyz` 가 앞서 이 상태에 **ready:true** 를 내고 있었다 — 표가 서 있는 것과 채워진 것을 구분하게 고쳤다 |
| [storyboard.html](storyboard.html) | 전원 | **데모 흐름 스토리보드(브라우저로 열기)** |

---

## 지금 바로 쓸 수 있는 것

```bash
# 서버 띄우기 — 고객(8080)과 운영(8081)은 **다른 앱**이다
python -m scripts.run_customer_server   # :8080  관리자 API 가 아예 안 실린다
python -m scripts.run_admin_server      # :8081  관리자 API + 대시보드

# 무엇을 지원하는지
curl 127.0.0.1:8080/v1/support-manifest

# 판정 요청 — 가입일은 YYYYMMDD 다(ISO 로 보내면 422)
curl -X POST 127.0.0.1:8080/v1/prechecks -H 'Content-Type: application/json' -d '{
  "insurer": "DB손해보험",
  "enrolled_on": "20200301",
  "kcd_codes": ["F32", "E66", "S72"],
  "product_name": "프로미라이프 실손의료비"
}'
```

> ★2026-08-24부터 `app.main:app`도 최소 권한 `customer`다. 과거에는 `full`이라
> 관리자 API까지 한 프로세스에 올라왔지만, role 오타와 기본 export를 fail-closed로 고쳤다.
> 그래도 포트·앱 역할을 명시하고 운영 검증을 일관되게 하려면 위의
> `scripts.run_customer_server`·`scripts.run_admin_server`를 정식 진입점으로 쓴다.
>
> ★`localhost` 대신 `127.0.0.1` 을 쓴다. 서버가 IPv4 에만 바인딩돼 있어
> `localhost` 가 IPv6(`::1`)로 풀리면 붙지 못한다(실측에서 한 번 헛짚었다).

---

## 리랭커 — 붙는 자리가 둘이고, 기본은 꺼져 있다

조항 벡터 색인은 122,772조각이 적재돼 있지만 **오래도록 서비스 API 가 조회하지 않았다.**
2026-08-04 에 조항 의미검색을 신설해 처음으로 이었다.

| 붙는 자리 | 스위치 | 경로 |
|---|---|---|
| 보험 조항 | `INSURANCE_CLAUSE_RERANK_ENABLED` (기본 `false`) | `/api/admin/clause-search` — **운영서버 전용** |
| 커머스 RAG | `RAG_RERANK_ENABLED` (기본 `false`) | `/api/rag/*` — 걷어낼 잔재 |

> ★**스위치를 합치지 말 것.** 커머스를 지울 때 보험이 함께 꺼진다.
> 모델 설정(`RERANKER_*`)만 공유한다.
>
> ★**꺼진 채로 `rerank=true` 를 보내면 409 다.** 조용히 무시하고 200 을 주지 않는다.
> 리랭킹이 실패하면 **503** 이고, 벡터 순서로 되돌리지 않는다 —
> 되돌리면 「재정렬했다」고 믿으면서 실제로는 안 한 상태가 된다.

**실측**(417질의 · RTX 4070 SUPER · [초기 리포트](../reports/2026-08-05_0100_리랭커_붙는자리_실측.md))

```
Qwen3-Reranker-4B   hit@1  0.4844 → 0.5851  (+10.07%p)   지연 p50 2,717ms
두 붙는 자리의 hit@k 가 전부 같다 — 배선이 일치한다
(mrr 만 4.3e-5 차이. 질의 한 건의 동점 정렬 순서다)
```

★**기본이 꺼져 있는 이유는 지연이다.** 질의당 2.7초는 동기 요청에 넣기 무겁고,
12GB VRAM 에 4B fp16 이 11.9GB 로 빠듯하게 올라간다.

★★**P1 "판정 경로에 켤지" 결정 완료(2026-08-25)** — [417질의 GPU실측·종단검증](../reports/2026-08-25_2200_리랭커_417질의_GPU실측과_종단검증.md).
다른 GPU(RTX 2000 Ada)에서 소수 4자리까지 재현(hit@1 0.6379, chunk 채점 기준),
`chunk`(조각) 채점이 `full_clause` 보다 +5.04%p 높아 **`CLAUSE_RERANK_SCORE_BODY=chunk`
가 기본**. 결론: **`/v1/prechecks`(판정 경로)에는 켜지 않는다**(질병기호 대조는
결정적이라 리랭킹이 필요 없고, 2.8초를 얹을 이유가 없다) — **GPU 있는 배포에서만
`.env` 로 켠다.** 필요한 환경변수·타임아웃 값은 리포트 §4 에 그대로 있다.
`docs/TODO/reranker_20260804-2109.md` 의 P1 은 이걸로 완료.

```bash
# 붙는 자리를 전부 같은 후보셋으로 잰다(배포 어댑터를 그대로 부른다)
python -m scripts.eval.rerank_attachpoints --list
```

---

## 어느 판의 숫자인가 — 기준선은 manifest 가 갖는다

수치를 인용할 때 **어느 스키마 판인지 함께 적는다.** 실제로 s5 값을 s6 인 양 인용해
한 번 헛짚었다(치과 커버리지 154 → 4,181. 전처리가 좋아진 게 아니라 측정 패턴이 달랐다).

```bash
# 기준선 확인 — 입력·산출물·코드·설정·환경 해시가 한 자리에 박혀 있다
python -m scripts.extract.build_manifest --schema s6 --verify
```

| 파일 | 무엇 |
|---|---|
| `data/manifests/preprocess/manifest_s6.json` | **s6 기준선.** 문서 1,367 · 입력 PDF SHA-256(원본 재해시 대조) · `s5`/`s6` 산출물 해시 · extractor·config·의존성·**git commit + dirty 해시** |
| `data/raw/manifests/*.jsonl` | **수집 기록**(URL 2,121행). 축이 다르다 — 저건 입력의 계보, 위는 **산출물의 계보** |

`--verify` 는 기록된 해시를 실제 파일과 다시 대조한다. 산출물이 1바이트라도 바뀌면 실패한다.
**manifest 는 불변이다** — 내용이 다른 것을 같은 경로에 쓰려 하면 거부한다.

---

## 전처리 산출물을 직접 뒤져 보려면

집계는 [preprocess_viz.html](preprocess_viz.html) 로, **행 단위 탐색은 Parquet + Tad** 로 나눴다.
수십만 행을 단일 HTML 에 넣으면 파일 크기와 브라우저 메모리가 문제가 된다.

**Tad** — 무료(MIT) · DuckDB 기반 · Parquet 를 그냥 더블클릭해서 정렬·필터·피벗한다.
[tadviewer.com](https://www.tadviewer.com/) 에서 Windows 설치파일을 받는다.

| 파일 | 내용 |
|---|---|
| `data/exports/s6_clauses.parquet` | **204,098행 · 22컬럼 · 57MB.** 조항 본문 + `reuse_docs`(재사용 문서 수) · `para_ambiguous` · `statute` |
| `data/exports/s6_documents.parquet` | 1,367행. 문서별 쪽수·조항수·항수·suspect·모호·미해결 |
| `data/exports/views/v1~v5.parquet` | ★**행 단위로만 보이는 것 5개.** HTML §H 의 캡처와 같은 화면이 그대로 열린다 |

```
v1_clause_boundary  조항 경계 붕괴 (30,000자 초과)     7행   ← s5 133행에서 급감
v2_page_fallback    페이지 덩어리 — 조항이 아니다      438행
v3_reuse            170문서에 실린 같은 조항         2,962행
v4_para_marker      항 표지 충돌                   2,843행
v5_pua              못 읽은 보조 PUA — 스니펫 포함    366행
```

`s6_clauses.parquet` 에서 `reuse_docs` 내림차순 정렬 → 중복 조항,
`para_ambiguous=true` 필터 → 인용 못 대는 조항 **8,554건**(s6)이 바로 나온다.
`unresolved>0` 은 366건이다.

> ★위 수치는 전부 **s6 기준**이다. 앞서 이 문단이 s5 값(9,838)을 달고 있었다.
> 스키마가 바뀌면 **분모가 바뀐다** — 인용할 때 어느 판인지 함께 적는다.

---

## 이 프로젝트의 제1원칙

**모르면 모른다고 한다.**

"보장됩니다"라고 잘못 말하면 사용자가 청구했다가 거절당하거나, 받을 걸 포기한다.
그래서 정확도보다 **정직성**이 앞선다.

- 근거 조항을 못 대면 `verdict="needs_expert"` · `abstained=true` 로 답한다. 이건 **정상 결과**다(HTTP 200).
- **면책 목록에 없다 ≠ 보장된다.** 보장은 '보상하는 사항' 조항이 정한다.
- 추론과 사실을 구분해 저장한다(`date_confidence`, `inferred`, `verification`).
- 외부에서 받은 데이터를 약관과 같은 근거로 쓰지 않는다(`evidence_tier`).

자세한 것은 저장소 루트 [CLAUDE.md](../../CLAUDE.md) 를 보라.
